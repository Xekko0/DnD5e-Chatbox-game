/**
 * Item Detector — PLAN_V5 §4.3, §5
 * LLM call #3, conditional (pre-filter keyword: "nhặt", "thấy", "tìm", "rương")
 * Output JSON: { detected_items: [{ name, type, quantity, properties, confidence }] }
 * Renderer: FloatingItemCard trong chat
 * TODO: Implement (prompt + few-shot VN, Zod schema, card spawn)
 */
export {};
