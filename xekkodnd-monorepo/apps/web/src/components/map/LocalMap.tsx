/**
 * Local Map (Tier 2) — PLAN_V5 §4.5
 * Battlemap grid 20×20–40×40, 1 ô = 5-30 ft
 * Tile inventory (items trên map tile, visible khi adjacent)
 * Entity positions (player, NPC, monster icons)
 * Tactical combat overlay (M2+)
 * Click tile → info, double-click → move, right-click → context menu
 * TODO: Implement
 */
export default function LocalMap() {
  return null;
}
