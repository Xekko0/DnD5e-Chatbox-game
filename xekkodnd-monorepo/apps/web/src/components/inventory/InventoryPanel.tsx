/**
 * Inventory Panel — PLAN_V5 §4.3 (Trụ 3: Interactive Items)
 * Hiển thị inventory list + equipment slots
 * Drag-drop: @dnd-kit/core
 *   - Item card chat → Inventory list
 *   - Item card chat → Equipment slot (pickup + equip)
 *   - Inventory → Equipment slot
 *   - Inventory → Chat (give NPC, drop)
 * TODO: Implement
 */
export default function InventoryPanel() {
  return null;
}
